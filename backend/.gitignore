# Local environment overrides — never commit these
/config/params-local.php
/config/db-local.php

# phpstorm project files
.idea

# visual studio code project files
.vscode

# netbeans project files
nbproject

# zend studio for eclipse project files
.buildpath
.project
.settings

# windows thumbnail cache
Thumbs.db

# composer vendor dir
/vendor

# composer itself is not needed
composer.phar

# Mac DS_Store Files
.DS_Store

# phpunit itself is not needed
phpunit.phar
# local phpunit config
/phpunit.xml

tests/_output/*
tests/_support/_generated

#vagrant folder
/.vagrant

# runtime and uploads
/runtime/*
!/runtime/.gitkeep
/web/assets/*
!/web/assets/.gitkeep
/web/uploads/images/*
!/web/uploads/images/.gitkeep
/web/uploads/resumes/*
!/web/uploads/resumes/.gitkeep
/web/uploads/seeker-resumes/*
!/web/uploads/seeker-resumes/.gitkeep
/web/uploads/job-docs/*
!/web/uploads/job-docs/.gitkeep